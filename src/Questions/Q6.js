// Implement a functional component called "Person" that receives a "name" prop and displays "Hello, [name]."

import React from 'react'

const ObjectProp = ({name}) => {
  return (
    <div>
        <h4>Q6. Implement a functional component that receives a "name" prop and displays "Hello, [name]."</h4>
        <h1>Hello, {name}.</h1>
    </div>
  )
}

export default ObjectProp;